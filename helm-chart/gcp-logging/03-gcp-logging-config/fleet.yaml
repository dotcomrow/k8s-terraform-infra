# Cluster-scoped bundle must NOT use defaultNamespace or targetNamespace
defaultNamespace: gcp-logging-system
name: 03-gcp-logging-config

dependsOn:
  - name: 01-gcp-logging-namespace
    namespace: kube-system
  - name: 02-gcp-logging-secrets
    namespace: gcp-logging-system